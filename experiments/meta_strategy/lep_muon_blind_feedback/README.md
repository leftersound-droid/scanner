# LEP muon blind strategy feedback

Real-data method test using DELPHI muon-pair angular distributions for discovery and OPAL muon-pair angular distributions as an external blind validator. The Scanner analyzer receives DELPHI data only; OPAL is consumed only by the experiment-level validator after each scan is complete. No particle-model force law or R4 operator modification is introduced.
